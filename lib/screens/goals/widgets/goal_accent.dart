/// Goal accent colour used for visual theming of goal cards and sections.
///
/// Each accent maps to a specific hex colour used throughout the UI.
enum GoalAccent {
  purple,
  blue,
  green,
  orange,
  cyan,
  pink,
  yellow,
  red,
}
